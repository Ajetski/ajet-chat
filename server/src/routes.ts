import { Router } from 'express';

const router = Router();

//router.get('/posts/media/:id', async (req, res) => {
//const post = await getPostsById(+req.params.id);

//res.writeHead(200, {
//'Content-Type': 'image/png',
//'Content-Length': post.media.length,
//});
//res.end(post.media);
//});

export default router;
